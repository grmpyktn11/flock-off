# Expo HAS CHANGED

This app is pinned to Expo SDK 54, not the latest. Read the exact
versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing
any code. The pin exists because the test phone's Expo Go supports 54
only.
